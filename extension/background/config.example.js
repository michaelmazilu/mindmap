/**
 * Copy this file to `config.local.js` (gitignored) and set your Modal HTTPS URL.
 * Shown after: modal deploy modal_app.py  (ends in .modal.run, not modal.com/apps)
 *
 *   cp config.example.js config.local.js
 *   # edit MINDMAP_HOSTED_ENDPOINT
 */
self.MINDMAP_HOSTED_ENDPOINT = '';
